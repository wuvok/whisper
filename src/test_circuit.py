
import sys
import pytest 
import wechsel 
import circuit 

    # To-do: if error thrown then frisbee second db first and backup to the hidden encrypted 
def test_iterator() -> int:
    from random import randint
    assert type(circuit.iterator(9,15)) is int
    return randint(9,15)
    
def test_combine():
    circuit.combine(9,15)
    assert len(wechsel.Components().generic) == test_iterator 
   
def test_charm():
    ...