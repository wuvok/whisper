
from collections.abc import Hashable




class Components():
    #To-do: configure encapsulation! 
    def __init__(self):
         self.duration = None 
         self.password = ()
         self.identity = ()
    def email_address(self,email) -> str:
        # todo-- create db table foreign reference 
        # todo-- map emails for table atomicity 
        # todo-- jacard for formality 
        self.email = email 
        if():
            return 
        else:
            return 
          
    def gen_sequence(self, generic = list ): 
        self.generic = generic 
        if(generic is list):
            self.password = (i for i in generic)

    def clipper(self):
         
        from pyperclip import clip
        
        
    

